<%= simple_form_for [@job, @comment] do |f| %>
  <%= f.input :text, :label => 'Вопрос', :as => :text,
      :input_html => { :class => 'span8' } %>
  <%= f.button :submit, 'Создать', :class => 'btn-success' %>
<% end %>
